{
  "name": "docker-todo-app",
  "version": "1.0.0",
  "description": "Full-stack Todo app with Express + HTML, Dockerized",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
